# Runtime API path — use /api/v1 in dev (Vite proxies it), full URL in production
VITE_API_BASE_URL=/api/v1

# Dev proxy target — Vite forwards /api/* requests here (dev only, not bundled)
VITE_API_PROXY_TARGET=https://openclick-backend-production.up.railway.app
